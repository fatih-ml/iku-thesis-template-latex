\section{\bilingual{Tablolar}{Tables}}
\bilingual{%
  Her tablo \texttt{tables/} klasöründe ayrı bir dosyada tutulur ve
  \texttt{\textbackslash inputtable} ile eklenir.
  Tablo başlıkları tablonun \emph{üstünde} yer alır.
  Örnek için bkz.\ Tablo~\ref{tab:example}.}{%
  Each table is kept in its own file under \texttt{tables/} and included with
  \texttt{\textbackslash inputtable}.
  Table captions appear \emph{above} the table.
  See Table~\ref{tab:example} for an example.}

\inputtable{example-results}
